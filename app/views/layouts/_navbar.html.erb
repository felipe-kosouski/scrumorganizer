<nav class="navbar navbar-expand-lg navbar-light bg-light justify-content-between">
  <%= link_to "Scrum Organizer", root_path, class: "navbar-brand" %>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto justify-content-end">

        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="http://example.com" id="navbarDropdownMenuLink"
               data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= current_user.username %>
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
              <%= link_to 'Dashboard', edit_user_registration_path,
                          class: 'dropdown-item' %>
              <%= link_to 'Sair', destroy_user_session_path, method: :delete,
                          class: 'dropdown-item' %>
            </div>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Registrar', new_user_registration_path, :class => "nav-link btn btn-outline-primary" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Entrar', new_user_session_path, :class => "nav-link btn btn-outline-primary" %>
          </li>
        <% end %>
      </ul>
    </div>

</nav>